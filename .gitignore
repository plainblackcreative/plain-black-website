.env
.env.*
!.env.example
.DS_Store
.claude/

# Node / Cloudflare Workers
node_modules/
.wrangler/

# Client work lives in the separate plainblack-client repo. Block it here
# so a stray `git add -A` can't push BLST/taca/etc. into the public site.
custom/

# OG image working files. The _-source-*.png files are the 1536x1024 originals
# that get cropped to the committed pb-*-og-1200x630.{jpg,webp} outputs.
# Keep them local but don't ship.
**/_*-source-*.png
